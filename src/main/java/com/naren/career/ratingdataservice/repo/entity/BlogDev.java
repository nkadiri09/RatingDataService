/*
package com.naren.career.ratingdataservice.repo.entity;

import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;

@Entity(name="blogDev")
public class BlogDev extends Blog{

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private int id;

    public BlogDev() {

    }

    public int getId() {
        return id;
    }
    public BlogDev(String title, String content) {
        super();
        this.setTitle(title);
        this.setContent(content);
    }
}
*/
